# Prompt Used

> Write a Kubernetes Operator in Go that manages Helm chart releases.

This is an example of a **poor prompt** — vague, no context, no constraints, no design requirements.

See `../second/PROMPT.md` for the well-written version and compare the resulting code quality.
